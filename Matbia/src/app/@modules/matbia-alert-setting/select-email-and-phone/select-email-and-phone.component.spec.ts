import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SelectEmailAndPhoneComponent } from './select-email-and-phone.component';

describe('SelectEmailAndPhoneComponent', () => {
  // let component: SelectEmailAndPhoneComponent;
  // let fixture: ComponentFixture<SelectEmailAndPhoneComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [SelectEmailAndPhoneComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(SelectEmailAndPhoneComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
