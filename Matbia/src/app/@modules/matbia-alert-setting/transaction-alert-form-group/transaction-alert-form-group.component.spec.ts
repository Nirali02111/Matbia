import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TransactionAlertFormGroupComponent } from './transaction-alert-form-group.component';

describe('TransactionAlertFormGroupComponent', () => {
  // let component: TransactionAlertFormGroupComponent;
  // let fixture: ComponentFixture<TransactionAlertFormGroupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [TransactionAlertFormGroupComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(TransactionAlertFormGroupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
