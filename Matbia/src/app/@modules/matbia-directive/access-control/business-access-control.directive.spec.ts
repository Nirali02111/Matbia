import { BusinessAccessControlDirective } from './business-access-control.directive';

describe('BusinessAccessControlDirective', () => {
  it('should create an instance', () => {
    // const directive = new BusinessAccessControlDirective();
    expect(true).toBeTruthy();
  });
});
