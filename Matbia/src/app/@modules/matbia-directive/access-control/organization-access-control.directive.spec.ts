import { OrganizationAccessControlDirective } from './organization-access-control.directive';

describe('OrganizationAccessControlDirective', () => {
  it('should create an instance', () => {
    // const directive = new OrganizationAccessControlDirective();
    expect(true).toBeTruthy();
  });
});
