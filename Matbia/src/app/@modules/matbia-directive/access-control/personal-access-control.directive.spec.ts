import { PersonalAccessControlDirective } from './personal-access-control.directive';

describe('PersonalAccessControlDirective', () => {
  it('should create an instance', () => {
    // const directive = new PersonalAccessControlDirective();
    expect(true).toBeTruthy();
  });
});
