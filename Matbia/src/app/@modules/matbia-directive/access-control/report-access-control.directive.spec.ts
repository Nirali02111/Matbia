import { ReportAccessControlDirective } from './report-access-control.directive';

describe('ReportAccessControlDirective', () => {
  it('should create an instance', () => {
    // const directive = new ReportAccessControlDirective();
    expect(true).toBeTruthy();
  });
});
