import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AccountHashComponent } from './account-hash.component';

describe('AccountHashComponent', () => {
  // let component: AccountHashComponent;
  // let fixture: ComponentFixture<AccountHashComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [AccountHashComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(AccountHashComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
