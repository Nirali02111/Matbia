import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CopyDataActionButtonComponent } from './copy-data-action-button.component';

describe('CopyDataActionButtonComponent', () => {
  // let component: CopyDataActionButtonComponent;
  // let fixture: ComponentFixture<CopyDataActionButtonComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [CopyDataActionButtonComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(CopyDataActionButtonComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
