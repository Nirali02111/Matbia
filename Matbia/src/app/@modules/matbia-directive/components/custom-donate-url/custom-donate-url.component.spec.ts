import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CustomDonateURLComponent } from './custom-donate-url.component';

describe('CustomDonateURLComponent', () => {
  // let component: CustomDonateURLComponent;
  // let fixture: ComponentFixture<CustomDonateURLComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [CustomDonateURLComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(CustomDonateURLComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
