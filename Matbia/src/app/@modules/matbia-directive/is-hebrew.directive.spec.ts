import { IsHebrewDirective } from './is-hebrew.directive';

describe('IsHebrewDirective', () => {
  it('should create an instance', () => {
    const directive = new IsHebrewDirective();
    expect(directive).toBeTruthy();
  });
});
