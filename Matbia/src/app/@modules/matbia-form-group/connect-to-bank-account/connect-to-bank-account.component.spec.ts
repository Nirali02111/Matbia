import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ConnectToBankAccountComponent } from './connect-to-bank-account.component';

describe('ConnectToBankAccountComponent', () => {
  // let component: ConnectToBankAccountComponent;
  // let fixture: ComponentFixture<ConnectToBankAccountComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [ConnectToBankAccountComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(ConnectToBankAccountComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
