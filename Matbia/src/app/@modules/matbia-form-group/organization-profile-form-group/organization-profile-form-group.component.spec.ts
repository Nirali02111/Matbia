import { ComponentFixture, TestBed } from '@angular/core/testing';

import { OrganizationProfileFormGroupComponent } from './organization-profile-form-group.component';

describe('OrganizationProfileFormGroupComponent', () => {
  // let component: OrganizationProfileFormGroupComponent;
  // let fixture: ComponentFixture<OrganizationProfileFormGroupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [OrganizationProfileFormGroupComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(OrganizationProfileFormGroupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
