import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ChangeCardPinPopupComponent } from './change-card-pin-popup.component';

describe('ChangeCardPinPopupComponent', () => {
  // let component: ChangeCardPinPopupComponent;
  // let fixture: ComponentFixture<ChangeCardPinPopupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [ChangeCardPinPopupComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(ChangeCardPinPopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
