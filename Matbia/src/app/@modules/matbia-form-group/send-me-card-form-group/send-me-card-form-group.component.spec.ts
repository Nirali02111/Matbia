import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SendMeCardFormGroupComponent } from './send-me-card-form-group.component';

describe('SendMeCardFormGroupComponent', () => {
  // let component: SendMeCardFormGroupComponent;
  // let fixture: ComponentFixture<SendMeCardFormGroupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [SendMeCardFormGroupComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(SendMeCardFormGroupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
