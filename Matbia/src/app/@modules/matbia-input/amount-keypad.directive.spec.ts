import { AmountKeypadDirective } from './amount-keypad.directive';

describe('AmountKeypadDirective', () => {
  it('should create an instance', () => {
    const directive = new AmountKeypadDirective();
    expect(directive).toBeTruthy();
  });
});
