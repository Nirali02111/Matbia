import { CardKeypadDirective } from './card-keypad.directive';

describe('CardKeypadDirective', () => {
  it('should create an instance', () => {
    const directive = new CardKeypadDirective();
    expect(directive).toBeTruthy();
  });
});
