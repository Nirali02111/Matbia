import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ScheduleRowItemComponent } from './schedule-row-item.component';

describe('ScheduleRowItemComponent', () => {
  // let component: ScheduleRowItemComponent;
  // let fixture: ComponentFixture<ScheduleRowItemComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [ScheduleRowItemComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(ScheduleRowItemComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
