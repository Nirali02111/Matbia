import { TestBed } from '@angular/core/testing';

import { DynamicGridReportService } from './dynamic-grid-report.service';

describe('DynamicGridReportService', () => {
  // let service: DynamicGridReportService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(DynamicGridReportService);
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
