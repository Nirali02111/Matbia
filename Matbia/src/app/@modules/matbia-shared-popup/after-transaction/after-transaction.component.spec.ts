import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AfterTransactionComponent } from './after-transaction.component';

describe('AfterTransactionComponent', () => {
  // let component: AfterTransactionComponent;
  // let fixture: ComponentFixture<AfterTransactionComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [AfterTransactionComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(AfterTransactionComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
