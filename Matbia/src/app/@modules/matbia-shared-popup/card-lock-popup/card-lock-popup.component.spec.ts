import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CardLockPopupComponent } from './card-lock-popup.component';

describe('CardLockPopupComponent', () => {
  // let component: CardLockPopupComponent;
  // let fixture: ComponentFixture<CardLockPopupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [CardLockPopupComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(CardLockPopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
