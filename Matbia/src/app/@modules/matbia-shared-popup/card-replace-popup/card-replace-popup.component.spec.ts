import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CardReplacePopupComponent } from './card-replace-popup.component';

describe('CardReplacePopupComponent', () => {
  // let component: CardReplacePopupComponent;
  // let fixture: ComponentFixture<CardReplacePopupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [CardReplacePopupComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(CardReplacePopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
