import { ComponentFixture, TestBed } from '@angular/core/testing';

import { RequestCardPopupComponent } from './request-card-popup.component';

describe('RequestCardPopupComponent', () => {
  // let component: RequestCardPopupComponent;
  // let fixture: ComponentFixture<RequestCardPopupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [RequestCardPopupComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(RequestCardPopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
