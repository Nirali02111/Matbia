import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SmallDepositPopupComponent } from './small-deposit-popup.component';

describe('SmallDepositPopupComponent', () => {
  // let component: SmallDepositPopupComponent;
  // let fixture: ComponentFixture<SmallDepositPopupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [SmallDepositPopupComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(SmallDepositPopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
