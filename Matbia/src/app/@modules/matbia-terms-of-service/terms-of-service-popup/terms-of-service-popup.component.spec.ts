import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TermsOfServicePopupComponent } from './terms-of-service-popup.component';

describe('TermsOfServicePopupComponent', () => {
  // let component: TermsOfServicePopupComponent;
  // let fixture: ComponentFixture<TermsOfServicePopupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [TermsOfServicePopupComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(TermsOfServicePopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
