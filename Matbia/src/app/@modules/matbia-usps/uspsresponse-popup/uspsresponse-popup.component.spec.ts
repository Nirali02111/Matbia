import { ComponentFixture, TestBed } from '@angular/core/testing';

import { USPSResponsePopupComponent } from './uspsresponse-popup.component';

describe('USPSResponsePopupComponent', () => {
  // let component: USPSResponsePopupComponent;
  // let fixture: ComponentFixture<USPSResponsePopupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [USPSResponsePopupComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(USPSResponsePopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
