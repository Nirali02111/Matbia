import { ComponentFixture, TestBed } from '@angular/core/testing';

import { PlaidButtonComponent } from './plaid-button.component';

describe('PlaidButtonComponent', () => {
  // let component: PlaidButtonComponent;
  // let fixture: ComponentFixture<PlaidButtonComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [PlaidButtonComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(PlaidButtonComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
