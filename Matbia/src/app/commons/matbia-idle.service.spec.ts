import { TestBed } from '@angular/core/testing';

import { MatbiaIdleService } from './matbia-idle.service';

describe('MatbiaIdleService', () => {
  // let service: MatbiaIdleService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(MatbiaIdleService);
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
