import { MatbiaUrlHandlingStrategy } from './matbia-url-handling-strategy';

describe('MatbiaUrlHandlingStrategy', () => {
  it('should create an instance', () => {
    expect(new MatbiaUrlHandlingStrategy()).toBeTruthy();
  });
});
