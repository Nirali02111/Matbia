export enum AlertTypes {
  TRANSACTIONS = 'Transactions Alert',
  BALANCE = 'Balance Alert',
  MANUAL_ENTRY = 'Manual Entry Alert',
  REQUEST = 'Request Alert',
  DEPOSIT = 'Deposit Initiate Alerts',
}
