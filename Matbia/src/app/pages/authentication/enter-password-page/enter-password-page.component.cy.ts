import { EnterPasswordPageComponent } from './enter-password-page.component'

describe('EnterPasswordPageComponent', () => {
  it('should mount', () => {
    cy.mount(EnterPasswordPageComponent)
  })
})