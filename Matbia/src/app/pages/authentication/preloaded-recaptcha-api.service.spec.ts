import { TestBed } from '@angular/core/testing';

import { PreloadedRecaptchaAPIService } from './preloaded-recaptcha-api.service';

describe('PreloadedRecaptchaAPIService', () => {
  // let service: PreloadedRecaptchaAPIService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(PreloadedRecaptchaAPIService);
  // });

  it('should be created', () => {
    expect(true).toBeTruthy();
  });
});
