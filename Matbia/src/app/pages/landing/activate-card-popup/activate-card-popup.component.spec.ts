import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ActivateCardPopupComponent } from './activate-card-popup.component';

describe('ActivateCardPopupComponent', () => {
  // let component: ActivateCardPopupComponent;
  // let fixture: ComponentFixture<ActivateCardPopupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [ActivateCardPopupComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(ActivateCardPopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
