import { ComponentFixture, TestBed } from '@angular/core/testing';

import { DonateToOrganizationComponent } from './donate-to-organization.component';

describe('DonateToOrganizationComponent', () => {
  // let component: DonateToOrganizationComponent;
  // let fixture: ComponentFixture<DonateToOrganizationComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [DonateToOrganizationComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(DonateToOrganizationComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
