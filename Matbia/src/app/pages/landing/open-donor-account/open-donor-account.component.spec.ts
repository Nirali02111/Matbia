import { ComponentFixture, TestBed } from '@angular/core/testing';

import { OpenDonorAccountComponent } from './open-donor-account.component';

describe('OpenDonorAccountComponent', () => {
  // let component: OpenDonorAccountComponent;
  // let fixture: ComponentFixture<OpenDonorAccountComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [OpenDonorAccountComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(OpenDonorAccountComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
