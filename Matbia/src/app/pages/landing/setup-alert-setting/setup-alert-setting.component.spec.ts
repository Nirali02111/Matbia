import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SetupAlertSettingComponent } from './setup-alert-setting.component';

describe('SetupAlertSettingComponent', () => {
  // let component: SetupAlertSettingComponent;
  // let fixture: ComponentFixture<SetupAlertSettingComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [SetupAlertSettingComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(SetupAlertSettingComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
