import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SetupCardSettingComponent } from './setup-card-setting.component';

describe('SetupCardSettingComponent', () => {
  // let component: SetupCardSettingComponent;
  // let fixture: ComponentFixture<SetupCardSettingComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [SetupCardSettingComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(SetupCardSettingComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
