import { ComponentFixture, TestBed } from '@angular/core/testing';

import { SetupOrgSettingComponent } from './setup-org-setting.component';

describe('SetupOrgSettingComponent', () => {
  // let component: SetupOrgSettingComponent;
  // let fixture: ComponentFixture<SetupOrgSettingComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [SetupOrgSettingComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(SetupOrgSettingComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
