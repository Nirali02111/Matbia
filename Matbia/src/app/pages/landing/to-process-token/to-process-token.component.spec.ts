import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ToProcessTokenComponent } from './to-process-token.component';

describe('ToProcessTokenComponent', () => {
  // let component: ToProcessTokenComponent;
  // let fixture: ComponentFixture<ToProcessTokenComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [ToProcessTokenComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(ToProcessTokenComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
