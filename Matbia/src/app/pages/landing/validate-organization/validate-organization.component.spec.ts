import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ValidateOrganizationComponent } from './validate-organization.component';

describe('ValidateOrganizationComponent', () => {
  // let component: ValidateOrganizationComponent;
  // let fixture: ComponentFixture<ValidateOrganizationComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [ValidateOrganizationComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(ValidateOrganizationComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
