import { ComponentFixture, TestBed } from '@angular/core/testing';

import { DonorAdvisedFundDepositComponent } from './donor-advised-fund-deposit.component';

describe('DonorAdvisedFundDepositComponent', () => {
  // let component: DonorAdvisedFundDepositComponent;
  // let fixture: ComponentFixture<DonorAdvisedFundDepositComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [DonorAdvisedFundDepositComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(DonorAdvisedFundDepositComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
