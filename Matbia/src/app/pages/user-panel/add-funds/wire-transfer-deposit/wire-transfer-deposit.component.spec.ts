import { ComponentFixture, TestBed } from '@angular/core/testing';

import { WireTransferDepositComponent } from './wire-transfer-deposit.component';

describe('WireTransferDepositComponent', () => {
  // let component: WireTransferDepositComponent;
  // let fixture: ComponentFixture<WireTransferDepositComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [WireTransferDepositComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(WireTransferDepositComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
