import { ComponentFixture, TestBed } from '@angular/core/testing';

import { NoPlaidComponent } from './no-plaid.component';

describe('NoPlaidComponent', () => {
  // let component: NoPlaidComponent;
  // let fixture: ComponentFixture<NoPlaidComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [NoPlaidComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(NoPlaidComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
