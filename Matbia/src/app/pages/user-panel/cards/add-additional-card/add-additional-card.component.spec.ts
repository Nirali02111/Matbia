import { ComponentFixture, TestBed } from '@angular/core/testing';

import { AddAdditionalCardComponent } from './add-additional-card.component';

describe('AddAdditionalCardComponent', () => {
  // let component: AddAdditionalCardComponent;
  // let fixture: ComponentFixture<AddAdditionalCardComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [AddAdditionalCardComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(AddAdditionalCardComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
