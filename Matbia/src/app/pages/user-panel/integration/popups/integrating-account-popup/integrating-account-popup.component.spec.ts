import { ComponentFixture, TestBed } from '@angular/core/testing';

import { IntegratingAccountPopupComponent } from './integrating-account-popup.component';

describe('IntegratingAccountPopupComponent', () => {
  // let component: IntegratingAccountPopupComponent;
  // let fixture: ComponentFixture<IntegratingAccountPopupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [IntegratingAccountPopupComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(IntegratingAccountPopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
