import { ComponentFixture, TestBed } from '@angular/core/testing';

import { IntegratingPopupComponent } from './integrating-popup.component';

describe('IntegratingPopupComponent', () => {
  // let component: IntegratingPopupComponent;
  // let fixture: ComponentFixture<IntegratingPopupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [IntegratingPopupComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(IntegratingPopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
