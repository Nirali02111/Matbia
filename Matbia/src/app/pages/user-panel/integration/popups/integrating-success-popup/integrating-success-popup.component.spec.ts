import { ComponentFixture, TestBed } from '@angular/core/testing';

import { IntegratingSuccessPopupComponent } from './integrating-success-popup.component';

describe('IntegratingSuccessPopupComponent', () => {
  // let component: IntegratingSuccessPopupComponent;
  // let fixture: ComponentFixture<IntegratingSuccessPopupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [IntegratingSuccessPopupComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(IntegratingSuccessPopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
