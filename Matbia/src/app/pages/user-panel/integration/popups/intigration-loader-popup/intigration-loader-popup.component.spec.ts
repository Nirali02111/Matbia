import { ComponentFixture, TestBed } from '@angular/core/testing';

import { IntigrationLoaderPopupComponent } from './intigration-loader-popup.component';

describe('IntigrationLoaderPopupComponent', () => {
  // let component: IntigrationLoaderPopupComponent;
  // let fixture: ComponentFixture<IntigrationLoaderPopupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [IntigrationLoaderPopupComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(IntigrationLoaderPopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
