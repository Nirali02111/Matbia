import { ComponentFixture, TestBed } from '@angular/core/testing';

import { OrderTokenBookComponent } from './order-token-book.component';

describe('OrderTokenBookComponent', () => {
  // let component: OrderTokenBookComponent;
  // let fixture: ComponentFixture<OrderTokenBookComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [OrderTokenBookComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(OrderTokenBookComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
