import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CancelDepositPopupComponent } from './cancel-deposit-popup.component';

describe('CancelDepositPopupComponent', () => {
  // let component: CancelDepositPopupComponent;
  // let fixture: ComponentFixture<CancelDepositPopupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [CancelDepositPopupComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(CancelDepositPopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
