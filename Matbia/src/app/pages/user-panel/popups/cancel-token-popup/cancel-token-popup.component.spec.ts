import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CancelTokenPopupComponent } from './cancel-token-popup.component';
import { FormsModule, ReactiveFormsModule } from '@angular/forms';

describe('CancelTokenPopupComponent', () => {
  // let component: CancelTokenPopupComponent;
  // let fixture: ComponentFixture<CancelTokenPopupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     imports: [CancelTokenPopupComponent, ReactiveFormsModule, FormsModule],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(CancelTokenPopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
