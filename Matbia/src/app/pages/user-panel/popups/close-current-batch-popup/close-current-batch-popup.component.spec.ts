import { ComponentFixture, TestBed } from '@angular/core/testing';

import { CloseCurrentBatchPopupComponent } from './close-current-batch-popup.component';

describe('CloseCurrentBatchPopupComponent', () => {
  // let component: CloseCurrentBatchPopupComponent;
  // let fixture: ComponentFixture<CloseCurrentBatchPopupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [CloseCurrentBatchPopupComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(CloseCurrentBatchPopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
