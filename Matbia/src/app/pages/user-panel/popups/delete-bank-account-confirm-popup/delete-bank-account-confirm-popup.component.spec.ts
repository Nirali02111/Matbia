import { ComponentFixture, TestBed } from '@angular/core/testing';

import { DeleteBankAccountConfirmPopupComponent } from './delete-bank-account-confirm-popup.component';

describe('DeleteBankAccountConfirmPopupComponent', () => {
  // let component: DeleteBankAccountConfirmPopupComponent;
  // let fixture: ComponentFixture<DeleteBankAccountConfirmPopupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [DeleteBankAccountConfirmPopupComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(DeleteBankAccountConfirmPopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
