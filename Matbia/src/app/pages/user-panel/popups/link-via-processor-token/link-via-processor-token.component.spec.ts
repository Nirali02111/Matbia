import { ComponentFixture, TestBed } from '@angular/core/testing';

import { LinkViaProcessorTokenComponent } from './link-via-processor-token.component';

describe('LinkViaProcessorTokenComponent', () => {
  // let component: LinkViaProcessorTokenComponent;
  // let fixture: ComponentFixture<LinkViaProcessorTokenComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [LinkViaProcessorTokenComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(LinkViaProcessorTokenComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
