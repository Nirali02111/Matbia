import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TokenEmailPopupComponent } from './token-email-popup.component';

describe('TokenEmailPopupComponent', () => {
  // let component: TokenEmailPopupComponent;
  // let fixture: ComponentFixture<TokenEmailPopupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [TokenEmailPopupComponent],
  //   }).compileComponents();

  //   fixture = TestBed.createComponent(TokenEmailPopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
