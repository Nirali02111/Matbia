import { ComponentFixture, TestBed } from '@angular/core/testing';

import { ProcessCardConfirmPopupComponent } from './process-card-confirm-popup.component';

describe('ProcessCardConfirmPopupComponent', () => {
  // let component: ProcessCardConfirmPopupComponent;
  // let fixture: ComponentFixture<ProcessCardConfirmPopupComponent>;

  // beforeEach(async () => {
  //   await TestBed.configureTestingModule({
  //     declarations: [ProcessCardConfirmPopupComponent],
  //   }).compileComponents();
  // });

  // beforeEach(() => {
  //   fixture = TestBed.createComponent(ProcessCardConfirmPopupComponent);
  //   component = fixture.componentInstance;
  //   fixture.detectChanges();
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
