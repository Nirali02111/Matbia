import { TestBed } from '@angular/core/testing';

import { AlertSettingAPIService } from './alert-setting-api.service';

describe('AlertSettingAPIService', () => {
  // let service: AlertSettingAPIService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(AlertSettingAPIService);
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
