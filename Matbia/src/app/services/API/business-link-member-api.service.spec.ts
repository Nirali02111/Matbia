import { TestBed } from '@angular/core/testing';

import { BusinessLinkMemberAPIService } from './business-link-member-api.service';

describe('BusinessLinkMemberAPIService', () => {
  // let service: BusinessLinkMemberAPIService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(BusinessLinkMemberAPIService);
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
