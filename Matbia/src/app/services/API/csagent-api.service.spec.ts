import { TestBed } from '@angular/core/testing';

import { CSAgentAPIService } from './csagent-api.service';

describe('CSAgentAPIService', () => {
  // let service: CSAgentAPIService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(CSAgentAPIService);
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
