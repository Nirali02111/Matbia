import { TestBed } from '@angular/core/testing';

import { DonationRequestAPIService } from './donation-request-api.service';

describe('DonationRequestAPIService', () => {
  // let service: DonationRequestAPIService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(DonationRequestAPIService);
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
