import { TestBed } from '@angular/core/testing';

import { DonorTokenAPIService } from './donor-token-api.service';

describe('DonorTokenAPIService', () => {
  // let service: DonorTokenAPIService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(DonorTokenAPIService);
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
