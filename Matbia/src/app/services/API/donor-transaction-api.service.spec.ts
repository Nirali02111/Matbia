import { TestBed } from '@angular/core/testing';

import { DonorTransactionAPIService } from './donor-transaction-api.service';

describe('DonorTransactionAPIService', () => {
  // let service: DonorTransactionAPIService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(DonorTransactionAPIService);
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
