import { TestBed } from '@angular/core/testing';

import { MatbiaAPIService } from './matbia-api.service';

describe('MatbiaAPIService', () => {
  // let service: MatbiaAPIService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(MatbiaAPIService);
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
