import { TestBed } from '@angular/core/testing';

import { MatbiaCardAPIService } from './matbia-card-api.service';

describe('MatbiaCardAPIService', () => {
  // let service: MatbiaCardAPIService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(MatbiaCardAPIService);
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
