import { TestBed } from '@angular/core/testing';

import { MatbiaCardSettingService } from './matbia-card-setting.service';

describe('MatbiaCardSettingService', () => {
  // let service: MatbiaCardSettingService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(MatbiaCardSettingService);
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
