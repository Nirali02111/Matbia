import { TestBed } from '@angular/core/testing';

import { OrganizationTokenAPIService } from './organization-token-api.service';

describe('OrganizationTokenAPIService', () => {
  // let service: OrganizationTokenAPIService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(OrganizationTokenAPIService);
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
