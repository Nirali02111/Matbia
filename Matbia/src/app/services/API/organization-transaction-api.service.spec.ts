import { TestBed } from '@angular/core/testing';

import { OrganizationTransactionAPIService } from './organization-transaction-api.service';

describe('OrganizationTransactionAPIService', () => {
  // let service: OrganizationTransactionAPIService;

  // beforeEach(() => {
  //   TestBed.configureTestingModule({});
  //   service = TestBed.inject(OrganizationTransactionAPIService);
  // });

  it('should create', () => {
    expect(true).toBeTruthy();
  });
});
